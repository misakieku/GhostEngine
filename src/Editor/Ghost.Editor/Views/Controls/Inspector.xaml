<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Ghost.Editor.Views.Controls.Inspector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Ghost.Editor.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:inspector="using:Ghost.Editor.Core.Inspector"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Inspector Header  -->
        <Grid
            Grid.Row="0"
            Padding="12,12,8,12"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource ControlElevationBorderBrush}"
            BorderThickness="0,0,0,1"
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ContentPresenter
                x:Name="IconPresenter"
                Grid.Column="0"
                VerticalAlignment="Center" />
            
            <ContentPresenter
                x:Name="HeaderPresenter"
                Grid.Column="1"
                VerticalAlignment="Center" />

            <DropDownButton
                Grid.Column="2"
                Padding="2"
                Style="{ThemeResource ToolbarButton}">
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Text="Send" />
                    </MenuFlyout>
                </DropDownButton.Flyout>
                <FontIcon FontSize="12" Glyph="&#xF8B0;" />
            </DropDownButton>
        </Grid>

        <!--  Content  -->
        <ScrollView Grid.Row="1" x:Name="ContentScrollView">
            <StackPanel x:Name="InspectorContentContainer" Orientation="Vertical" Spacing="4" Padding="0,4,0,12" />
        </ScrollView>
    </Grid>
</UserControl>
